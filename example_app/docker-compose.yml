web:
  build: .
  environment:
    - VIRTUAL_HOST=demo.corso-javascript.it
    - HOSTNAME=demo.corso-javascript.it
    # exampleapp_db_1 generated by docker-compose
    - MONGOURL=mongodb://exampleapp_db_1:27017/example_app
  links:
    - db

db:
  image: mongo
  volumes:
    - db:/data/db
